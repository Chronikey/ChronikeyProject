<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <link rel="shortcut icon" href="ChronikeyLogo.png" type="image/x-icon">
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Email Form</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-black flex items-center justify-center min-h-screen">

  <form class="bg-gray-900 p-8 rounded-2xl shadow-lg w-full max-w-sm space-y-6" method="post" action="/PassChange" >
    <h2 class="text-white text-2xl font-semibold text-center">Enter Your Email</h2>
       <%
        if(EmailError){%>
            <p style="color: red;"><%= EmailError %></p>
            <%}
        %>
    <input
      type="email"
      placeholder="Email address"
      name="Mail" id="Mail"
      class="w-full p-3 rounded-lg bg-gray-800 text-white focus:outline-none focus:ring-2 focus:ring-purple-500"
      required
    />

    <div class="flex justify-between">
      <button
        type="button"
        onclick="history.back()"
        class="px-4 py-2 bg-gray-700 text-white rounded-lg hover:bg-gray-600"
      >
        Back
      </button>
      <button
        type="submit"
        class="px-4 py-2 bg-purple-600 text-white rounded-lg hover:bg-purple-700"
      >
        Submit
      </button>
    </div>
  </form>

</body>
</html>
